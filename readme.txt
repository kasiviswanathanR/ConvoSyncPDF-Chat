install miniconda
pip install -r requirements.txt in conda after rediracting to the project folder
after come to vs
cd env/Scripts
activate
cd../..
streamlit run app.py

if error occurs as replicate api then change api token of replicate in .env 